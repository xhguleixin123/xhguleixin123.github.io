# 面向对象的设计原则

## 开闭原则

> 对拓展开放，对修改封闭

```csharp
// 定义一个接口
interface IPatient{
    public void methed()
}

// 实现接口
// 第一个种方法
public class Patient : IPatient{
    private int level;
    public Patient(_level){
        level = _leve
    };
    public void methed(){
        if(leve == 0){
            pass;
        }
        if(leve == 1){
            pass;
        }
    }
}

//如果增加了一个等级，就需要修改Patient类中的methed方法。所以应该根据不同的类，实现相同的接口，从而增加可拓展性，不需要修改原来的代码，修改原来的方法，需要回去找到特定位置修改，但是这种方法可以快速增加功能。
public class PatientLevel1 : IPatient{
    public void methed(){
        Pass;
    }
}
public class PatientLevel2 : IPatient{
    public void methed(){
        Pass;
    }
}
// 如果出现第三个水平的PatientLevel3,就重新建立一个新类。
public class PatientLevel2 : IPatient{
    public void methed(){
        Pass;
    }
}
```

## 单一职责原则

> * 不要存在多于一个导致类变更的原因。
> * 一个类/接口/方法只负责一项职责。

* 优点：降低复杂度、提高可读性、提高系统的可维护性、降低更改难度。
* 缺点：一味追求单一职责，有时会引起类的大爆炸，不过接口和方法应该遵循这个原则。

```csharp
//一个类中方法负责两个职责，如果需要拓展，则需要修改代码，所以将职能单一化。
public class Bird {
    public void MainMoveMode(string birdName){
    	if(birdName == "鸵鸟"){
        	Console.WriteLine("用脚走");
    	}
    	else {
        	Console.WriteLine("用翅膀飞");
    	}
    }
}
//所以分出一个单一职能的类。
public class FlyBird : Bird {
    private string bridName;
    public void FlyBird(string _birdName){
        birdName = _birdName;
    }
    public void MainMoveMode() {
        Console.WriteLine("用翅膀飞");
    }
}
public class WalkBird : Bird {
    private string bridName;
    public void FlyBird(string _birdName){
        birdName = _birdName;
    }
    public void MainMoveMode() {
        Console.WriteLine("用脚走");
    }
}

```

